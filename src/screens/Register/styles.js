import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  backButton: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    marginTop: 30,
  },
  textFooter:{
    color: "#374151",
    fontSize:16,
    fontWeight:"bold"
  }
});
